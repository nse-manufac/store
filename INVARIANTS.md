# INVARIANTS — กฎที่ห้ามพัง

เอกสารนี้คือ **สัญญาความถูกต้อง** ของระบบบันทึกสต็อก (Stock-log.html)
ทุก PR ต้องถูกตรวจว่าไม่ละเมิดข้อใดข้อหนึ่งในนี้ ก่อนเสนอให้เจ้าของอนุมัติ

> ถ้าการแก้ปัญหาหนึ่งจำเป็นต้องละเมิดข้อใดข้อหนึ่ง **ห้ามแก้เอง** — ให้หยุด
> แล้วเขียนใน PR ว่าติดข้อไหน และเสนอทางเลือกให้เจ้าของตัดสิน

---

## A. ความถูกต้องของยอด

**A1 — สูตรยอดคงเหลือ**
`ยอดคงเหลือ = Σ qty ที่ direction='IN' − Σ qty ที่ direction='OUT'`
นับเฉพาะรายการที่ `!voided` **และ** `entity === entity ที่เลือกอยู่`
อ้างอิง: `balMap`, `cardRows`
ห้ามมีเส้นทางใดในโปรแกรมคำนวณยอดด้วยสูตรอื่น

**A2 — ปัดทศนิยมที่เดียว**
ทุกผลลัพธ์ที่เป็นยอดต้องผ่าน `round5()` (`Math.round(n*1e5)/1e5`)
การบวกลบ float ตรง ๆ โดยไม่ปัดจะทำให้ยอดเพี้ยนแบบสะสม เช่นได้ `0.30000000000000004`

**A3 — ทุก query ต้องกรอง entity**
บริษัทมีหลายนิติบุคคลใช้แอปเดียวกัน ทุก `computed` / ทุกตาราง / ทุกไฟล์ส่งออก
ที่อ่าน `txns` ต้องกรอง `t.entity === entity.value`
**การลืมข้อนี้ทำให้ยอดของนิติบุคคลหนึ่งไปโผล่ในอีกนิติบุคคลหนึ่ง** ซึ่งตรวจไม่เจอจากหน้าจอทันที

**A4 — ยอดติดลบเตือนได้ แต่ห้ามบล็อก**
`saveOut()` เตือนเมื่อยอดจะติดลบ แต่ยังให้บันทึกได้หลังยืนยัน
เพราะของจริงมีกรณีคีย์รับเข้าย้อนหลัง ห้ามเปลี่ยนเป็นห้ามบันทึกเด็ดขาด

---

## B. ร่องรอยตรวจสอบ (audit trail)

**B1 — ห้ามลบรายการเคลื่อนไหวออกจากระบบ**
การ "ยกเลิก" คือ `voided = true` + `void_reason` + อัปเดต `updated_at` เท่านั้น
ห้ามใช้ `splice` / `filter` เพื่อเอา txn ออกจาก `txns.value` ในทุกกรณี
อ้างอิง: `voidTxn()`

**B2 — ประวัติการปิดยอด PO ต้องไม่หาย**
ปิดยอด = `push` แถวใหม่เข้า `closes`
ยกเลิกการปิด = หาแถว active ล่าสุดแล้วตั้ง `voided = true` + เหตุผล
ห้ามลบแถวเดิมหรือแก้แถวเดิมให้กลายเป็นการปิดครั้งใหม่
อ้างอิง: `closePo()`, `unclosePo()`

**B3 — `created_at` และ `id` ห้ามแก้หลังสร้าง**
ทั้งคู่เป็นหลักฐานและเป็นกุญแจของ sync การแก้ทำให้เกิดรายการซ้ำบนเครื่องอื่น

**B4 — `lockLeaks` ห้ามลบทิ้งเงียบ ๆ**
`lockLeaks` จับรายการที่หลุดเข้ามาหลังปิด PO แล้ว (เกิดจากเครื่องที่ cache เวอร์ชันเก่าค้าง)
มันต้องแสดงให้คนเห็นเสมอ ห้าม auto-void ห้ามซ่อน — เพราะเป็นสัญญาณว่ามีเครื่องใช้เวอร์ชันเก่าอยู่

---

## C. การล็อก PO

**C1 — นิยามของ "PO ปิดแล้ว"**
เรียงแถว `closes` ของ PO นั้นตาม `updated_at` เอาแถวล่าสุด ถ้า `!voided` = ปิดอยู่
รองรับลำดับ ปิด → ยกเลิก → ปิดใหม่ ได้ไม่จำกัดรอบ
อ้างอิง: `closedPoSet`, `isPoClosed()`

**C2 — PO ที่ปิดแล้วต้องบล็อก 2 ทาง**
- `voidTxn()` — ยกเลิกรายการของ PO นั้นไม่ได้
- `saveOut()` — จ่ายออกอ้างอิง PO นั้นเพิ่มไม่ได้

ถ้าเพิ่มช่องทางบันทึกใหม่ในอนาคต **ต้องเพิ่มการเช็ก `isPoClosed()` ด้วยเสมอ**

**C3 — ล็อกอยู่ที่ทางเข้า ไม่ใช่ที่ sync**
เป็นการตัดสินใจโดยตั้งใจ อย่าย้ายการล็อกไปไว้ที่ชั้น sync
(ผลข้างเคียงที่ยอมรับแล้วคือรายการหลุดได้ จึงมี `lockLeaks` ไว้จับ — ดู B4)

---

## D. การซิงค์กับ Google Sheets

**D1 — `Content-Type` ต้องเป็น `text/plain;charset=utf-8`**
ไม่ใช่ `application/json`
เหตุผล: `application/json` ทำให้เบราว์เซอร์ยิง CORS preflight (OPTIONS)
ซึ่ง **Apps Script Web App ไม่รองรับ** และ sync จะพังทั้งระบบ
อ้างอิง: `api()` — มีคอมเมนต์กำกับไว้แล้ว

**D2 — `_dirty` เป็นสถานะภายในเครื่อง ห้ามส่งขึ้นเซิร์ฟเวอร์**
ต้องถอดออกด้วย `cleanTxn()` ก่อน push เสมอ

**D3 — ของที่ยังไม่ได้ส่ง ห้ามถูกทับ**
ใน `mergeIncoming()` / `mergeList()` ถ้า `local._dirty === true` ให้ `continue` ทันที
ห้ามให้ข้อมูลที่ pull มาทับ ไม่ว่า `updated_at` จะใหม่กว่าแค่ไหน
**ละเมิดข้อนี้ = งานที่พนักงานคีย์ไว้หายโดยไม่มีใครรู้**

**D4 — หลัง push สำเร็จ ต้องเขียน `updated_at` ทับด้วย `serverTime`**
ถ้านาฬิกาเครื่องเดินเร็วกว่าเซิร์ฟเวอร์ การแก้ไขจากเครื่องอื่นจะถูกมองข้ามตลอดไป
อ้างอิง: `doSync()` — มีคอมเมนต์กำกับไว้แล้ว

**D5 — เปรียบเทียบเวลาด้วยการเทียบ string ของ ISO 8601**
`String(a.updated_at) > String(b.updated_at)` ใช้ได้เพราะ ISO 8601 เรียงตามตัวอักษรได้
ถ้าเปลี่ยนรูปแบบวันที่เป็นอย่างอื่น (เช่น `DD/MM/YYYY`) การเรียงลำดับทั้งระบบจะพังเงียบ ๆ

**D6 — ตาราง POs / Shortages / Kits / Closes ต้องอยู่ใน try/catch แยก**
เพื่อให้เครื่องที่ Apps Script ยังเป็นเวอร์ชันเก่า **ยังซิงค์รายการรับเข้า-จ่ายออกได้ตามปกติ**
ห้ามรวมเข้า try เดียวกับ `pushTxns`/`pullTxns`

**D7 — เขียนลงเครื่องก่อนเสมอ (offline-first)**
ทุกการบันทึกต้อง `persist()` ลง localStorage ให้สำเร็จก่อน แล้วค่อย `doSync()`
ห้ามทำให้การบันทึกขึ้นกับผลของ network

---

## E. การเก็บข้อมูลในเครื่อง

**E1 — key ของ localStorage ห้ามเปลี่ยนโดยไม่มี migration**
`bincard.setup.v1` · `bincard.txns.v1` · `bincard.sync.v1`
ข้อมูลของพนักงานอยู่ในเครื่องเขา **เปลี่ยน key = ข้อมูลหายและกู้ไม่ได้**

**E2 — เปลี่ยนโครงสร้างข้อมูล = ต้องมี migration และต้องให้เจ้าของอนุมัติแยก**
เพิ่ม field ใหม่ที่มีค่า default ปลอดภัย ทำได้
เปลี่ยนชื่อ / ลบ / เปลี่ยนชนิดของ field เดิม ต้องเขียน migration ที่อ่านของเก่าได้
และต้องระบุให้ชัดใน PR ว่า **"PR นี้แตะโครงสร้างข้อมูล"**

**E3 — เก็บเฉพาะวัตถุดิบที่ `active === 'TRUE'`**
localStorage มีเพดาน ~5 MB และทะเบียนเต็ม 12,259 รายการกินไป ~2.5 MB
`persist()` จึงกรอง active ก่อนเซฟ เพื่อเหลือที่ให้รายการเคลื่อนไหว
ห้ามเซฟทะเบียนทั้งก้อนลงเครื่อง

---

## F. การ deploy

**F1 — push ขึ้น `main` = ขึ้น production ทันที**
GitHub Pages เผยแพร่ให้อัตโนมัติ ไม่มี staging ไม่มี build step
พนักงานที่กด refresh จะได้เวอร์ชันใหม่ทันที

**F2 — ต้องเป็นไฟล์เดียวจบ**
ห้ามแตกเป็นหลายไฟล์ ห้ามเพิ่ม build step ห้ามเพิ่ม npm
พนักงานต้องเปิดไฟล์เดียวแล้วใช้ได้ และต้องเซฟไฟล์ไปใช้แบบ offline ได้

**F3 — repo นี้เป็น public**
`.gitignore` เป็นแบบ deny-all แล้ว allow ทีละไฟล์ (`/*` แล้ว `!/ชื่อไฟล์`)
**ห้าม commit ข้อมูลธุรกิจ** (BOM, ทะเบียนวัตถุดิบ, ไฟล์ PO, ยอดจริง)
**ห้าม hardcode URL ของ Apps Script หรือ token** — ผู้ใช้ต้องกรอกเองเสมอ
ถ้าเพิ่มไฟล์ใหม่ ต้องเพิ่มบรรทัด allow ใน `.gitignore` ด้วย ไม่งั้นไฟล์จะไม่ถูก track

---

## G. ประสบการณ์ผู้ใช้ที่ห้ามเสีย

**G1 — ภาษาไทยทั้งหมด** ข้อความที่ผู้ใช้เห็นต้องเป็นภาษาไทย รวมถึงข้อความ error

**G2 — คีย์ด้วยคีย์บอร์ดอย่างเดียวได้**
หน้ารับเข้า/จ่ายออกออกแบบให้คีย์รัว ๆ ได้ ห้ามเพิ่ม modal หรือขั้นตอนที่บังคับให้ต้องใช้เมาส์
ห้ามทำลาย `focusEl()` / ลำดับ tab ที่มีอยู่

**G3 — ข้อความเตือนต้องบอกทางออก**
ไม่ใช่แค่ "ผิดพลาด" แต่ต้องบอกว่าให้ทำอะไรต่อ
เช่น `"PO ... ปิดยอดแล้ว — ยกเลิกการปิด PO ก่อน ถ้าต้องแก้"`
