# CLAUDE.md — คู่มือสำหรับ AI agent ที่เข้ามาแก้ repo นี้

**อ่าน [INVARIANTS.md](INVARIANTS.md) ให้จบก่อนแตะโค้ดทุกครั้ง** ไฟล์นั้นคือกฎที่ห้ามพัง
ไฟล์นี้คือแผนที่ว่าอะไรอยู่ตรงไหน และทำงานยังไงให้ปลอดภัย

---

## 1. ระบบนี้คืออะไร

ระบบบันทึกสต็อกวัตถุดิบ (bin card) ของโรงงาน ใช้แทนการ์ดกระดาษ
ผู้ใช้คือพนักงานคลังและฝ่ายผลิต ใช้บนคอมในโรงงาน ภาษาไทยล้วน

งานหลักที่ระบบทำ:
- **รับเข้า** วัตถุดิบตาม PO (ของมาไม่พร้อมกัน คีย์รับได้หลายรอบต่อ PO)
- **จ่ายออก** ให้ฝ่ายผลิต อ้างอิง PO
- **ดูการ์ด** ยอดคงเหลือรายรหัส พร้อมยอดสะสมทีละบรรทัด แบบเดียวกับการ์ดกระดาษ
- **ปิดยอด PO** หลังตรวจกระทบยอดเสร็จ แล้วล็อกไม่ให้คีย์เพิ่ม
- **คิวเคลม** จับกรณีรับของมาไม่ตรงกับที่ระบุในเอกสาร
- **ออก Bin Card เป็น Excel** ตาม template เดิมของโรงงาน
- **ซิงค์หลายเครื่อง** ผ่าน Google Sheets (ไม่บังคับ ใช้ offline ได้)

ไฟล์ในระบบ:

| ไฟล์ | หน้าที่ |
|---|---|
| `Stock-log.html` | แอปหลัก — ทุกอย่างอยู่ในไฟล์เดียว |
| `ทะเบียนวัตถุดิบ.html` | เครื่องมือจัดการทะเบียนวัตถุดิบ + BOM แยกต่างหาก |
| `google-apps-script.gs` | โค้ดฝั่งเซิร์ฟเวอร์ ผู้ใช้ต้องเอาไป deploy บน Google Apps Script เอง |

---

## 2. Deploy

**push ขึ้น `main` = ขึ้น production ทันที** ผ่าน GitHub Pages
ไม่มี build ไม่มี test runner ไม่มี staging → https://nse-manufac.github.io/store/

agent **ห้าม push ขึ้น `main` เด็ดขาด** ทุกการแก้ต้องเป็น branch + PR ให้เจ้าของอนุมัติ

---

## 3. Stack

- **Vue 3** (global build ไม่ใช่ SFC) — `createApp({ setup(){ ... return {...} } })`
- **XLSX (SheetJS)** อ่าน/เขียน Excel · **ExcelJS + JSZip** ออก Bin Card ตาม template
- ทั้งหมดโหลดจาก **cdnjs** ผ่าน `<script src>` ที่บรรทัด 7–10
- ไม่มี npm ไม่มี bundler ไม่มี TypeScript

⚠️ **ข้อจำกัดที่รู้อยู่:** เพราะ lib มาจาก cdnjs ถ้าเน็ตโรงงานล่มหรือบล็อก cdnjs
**แอปจะเปิดไม่ขึ้นเลย** (ต่างจาก `plan` ที่ vendor lib ไว้ในไฟล์)
ถ้าจะแก้เรื่องนี้ ถือเป็นงานใหญ่ ต้องให้เจ้าของตัดสินใจก่อน ห้ามทำแถมมากับ PR อื่น

---

## 4. หาที่แก้ยังไงโดยไม่ต้องอ่านทั้งไฟล์

`Stock-log.html` ยาว ~3,300 บรรทัด **ห้ามอ่านทั้งไฟล์เข้า context**
โค้ดแบ่งเป็นบล็อกด้วย section banner ให้ค้นด้วย pattern นี้:

```
grep -n "─────────\|═══════════" Stock-log.html
```

บล็อกหลัก (บรรทัดจะขยับ ให้ค้นด้วยชื่อ ไม่ใช่เลขบรรทัด):

| Section banner | มีอะไร |
|---|---|
| `state` | ตัวแปรหลักทั้งหมด · `K_SETUP` `K_TXN` `K_SYNC` |
| `ดัชนีเพื่อความเร็ว` | `matMap` `bomByPn` `entInfo` |
| `ยอดคงเหลือ` | `liveTxns` `balMap` `balOf` `round5` ← **หัวใจของ A1/A2** |
| `หน้ารับเข้า` | `saveIn()` สร้าง txn ทิศ IN |
| `หน้าจ่ายออก` | `saveOut()` สร้าง txn ทิศ OUT + เช็ก PO ปิด |
| `ดูการ์ด` | `cardRows` ยอดสะสมรายบรรทัด |
| `ค้นหา / แก้ไข` | `listRows` `voidTxn()` |
| `คิวเคลม` | `claims` |
| `บันทึก / ส่งออก` | `persist()` `sheetFromTxns()` export Excel |
| `ปิดยอด PO` | `closedPoSet` `isPoClosed` `poRecon` `closePo` `unclosePo` `lockLeaks` |
| `นำเข้าไฟล์ …` | parser ของไฟล์ PO / Kit List จาก PC ลูกค้า |
| `เชื่อมต่อ Google Sheets` | `api()` `mergeIncoming()` `mergeList()` `doSync()` |

HTML template อยู่ครึ่งบนของไฟล์ (ก่อนบรรทัด ~1440) · JavaScript อยู่ครึ่งล่าง

---

## 5. โครงสร้างข้อมูล

เก็บใน localStorage 3 key:

| key | เก็บอะไร |
|---|---|
| `bincard.setup.v1` | `materials` (เฉพาะ active) · `entities` · `bom` · `people` · `poList` · `shorts` · `kits` · `closes` |
| `bincard.txns.v1` | `txns` รายการเคลื่อนไหวทั้งหมด |
| `bincard.sync.v1` | การตั้งค่าเชื่อมต่อ Google Sheets (url, token, device) |

**รายการเคลื่อนไหว (txn)**

```js
{
  id: 'T' + Date.now() + '-' + random5,   // ห้ามแก้หลังสร้าง
  entity, direction: 'IN' | 'OUT',
  material_code, doc_ref /* = PO */, part_no,
  date, time, qty,
  reqmt_qty, issued_qty,                  // ใช้เทียบหาคิวเคลม
  person, expiry_date, remark, batch,
  created_at, updated_at,                 // ISO 8601 เสมอ
  voided: false, void_reason: '',          // ยกเลิกแบบไม่ลบ
  device,
  _dirty: true                            // ภายในเครื่องเท่านั้น ห้ามส่งขึ้นเซิร์ฟเวอร์
}
```

**การปิดยอด PO (close)**

```js
{ id, po, closed_at, device, note, voided, void_reason, updated_at, _dirty }
```

---

## 6. ขั้นตอนการทำงานที่ต้องทำตาม

1. **อ่าน INVARIANTS.md** แล้วระบุในหัว PR ว่าการแก้นี้ไปแตะหมวดไหนบ้าง (A–G)
2. **ทำซ้ำอาการก่อน** เขียนใน issue ว่าเกิดจากอะไร บรรทัดไหน ก่อนแก้
3. **แก้ให้เล็กที่สุด** แก้เฉพาะสาเหตุ ห้ามจัดระเบียบโค้ดรอบข้างมาด้วย
   PR ที่ diff เกิน ~80 บรรทัดจะรีวิวตอนพักเที่ยงไม่ทัน ถ้าจำเป็นต้องใหญ่กว่านั้นให้บอกเหตุผลไว้ในหัว PR
4. **บัมป์ `<meta name="app-version">` ทุกครั้งที่แก้ `Stock-log.html`** (INVARIANTS F4)
   รูปแบบ `YYYY-MM-DD.ลำดับ` — CI จะทำให้ PR แดงถ้าลืม
5. **หนึ่ง issue = หนึ่ง PR** ห้ามรวมหลายเรื่องใน PR เดียว
6. **เขียนสรุปเป็นภาษาไทย** หัว PR ต้องมี: อาการคืออะไร · เกิดจากอะไร · แก้ยังไง · ต้องทดสอบอะไร
7. **ห้าม push `main` · ห้าม merge เอง · ห้ามแก้ `.github/workflows/` เพื่อข้ามการตรวจ**

---

## 7. หยุดแล้วถามเจ้าของก่อน เมื่อเจอกรณีเหล่านี้

- ต้องเปลี่ยนโครงสร้างข้อมูลใน localStorage (INVARIANTS E2)
- ต้องเปลี่ยนสูตรคำนวณยอด หรือความหมายของการปิดยอด PO
- ต้องเปลี่ยนวิธี sync หรือ contract กับ `google-apps-script.gs`
  (ผู้ใช้ต้องไป re-deploy Apps Script เอง = ต้องแจ้งล่วงหน้า)
- ต้องเพิ่ม dependency ใหม่ หรือเพิ่มไฟล์ใหม่เข้า repo
- แก้ปัญหาไม่ได้โดยไม่ละเมิด INVARIANTS ข้อใดข้อหนึ่ง
- เรื่องที่แจ้งมาไม่ใช่บั๊กของโค้ด แต่เป็นข้อมูลตั้งต้นผิด หรือผู้ใช้ใช้ผิดวิธี
  → ตอบกลับผู้แจ้งเป็นภาษาไทย แล้วปิด issue ไม่ต้องเปิด PR

---

## 8. ทดสอบยังไง

### 8.1 Smoke test อัตโนมัติ

รันเองทุก PR ผ่าน `.github/workflows/smoke.yml` — **PR ที่เทสแดงห้ามเสนอให้เจ้าของอนุมัติ**

```bash
npm install && npx playwright install chromium && npm test
```

ชื่อเทสอ้างข้อกฎใน `INVARIANTS.md` ตรง ๆ (เช่น `A1 — ยอดคงเหลือ = ...`)
เทสแดงข้อไหน = ละเมิดกฎข้อนั้น ไปอ่านกฎก่อน อย่าเพิ่งไปแก้เทส

> `package.json` กับ `tests/` เป็น**เครื่องมือตอนพัฒนาเท่านั้น**
> ตัวแอปยังเป็นไฟล์เดียวไม่มี build ไม่มี dependency เหมือนเดิม (INVARIANTS F2)
> ห้ามเอา npm เข้ามายุ่งกับ `Stock-log.html`

**จุดยึดของเทส:** `document.querySelector('#app')._vnode.component.setupState`
เปิดทุกอย่างที่ `setup()` return ออกมา ⚠️ ค่าถูกแกะ ref แล้ว — เขียนค่าต้องใช้
`s.entity = 'E1'` **ห้ามใช้** `s.entity.value = 'E1'` (เงียบหายไม่มี error)

### 8.2 ทดสอบด้วยมือเพิ่ม (สำหรับสิ่งที่เทสยังไม่ครอบคลุม)

smoke test ครอบตรรกะยอด/audit trail/การล็อก PO แต่ **ยังไม่ครอบ UI**
ถ้า PR แตะหน้าจอ ให้ทดสอบด้วยมือแล้วเขียนผลใน PR:

1. เปิด `Stock-log.html` ในเบราว์เซอร์ ไม่ต้องตั้งค่า sync
2. โหลดทะเบียน + BOM ตัวอย่าง เลือกนิติบุคคล
3. **เส้นทางที่ต้องผ่านทุกครั้ง:**
   - รับเข้า 1 รายการ → ยอดคงเหลือเพิ่มถูกต้อง
   - จ่ายออก 1 รายการ → ยอดลดถูกต้อง · การ์ดแสดงยอดสะสมต่อเนื่อง
   - ยกเลิกรายการ → รายการยังอยู่ในตาราง (ขีดฆ่า) และยอดกลับไปเท่าเดิม
   - ปิดยอด PO → กดยกเลิกรายการของ PO นั้นไม่ได้ · จ่ายออกอ้าง PO นั้นไม่ได้
   - ยกเลิกการปิดยอด → กลับมาคีย์ได้ และประวัติการปิดยังอยู่ครบ
   - refresh หน้า → ข้อมูลทั้งหมดยังอยู่
4. เปิด DevTools Console ต้องไม่มี error

---

## 9. คำศัพท์

| คำ | หมายถึง |
|---|---|
| bin card / การ์ด | การ์ดคุมยอดวัตถุดิบรายรหัส |
| entity / นิติบุคคล | บริษัทในเครือ ข้อมูลแยกกันเด็ดขาด |
| PO (`doc_ref`) | ใบสั่งซื้อ ใช้เป็นตัวอ้างอิงของทั้งรับเข้าและจ่ายออก |
| PN (`part_no`) | รหัสสินค้าที่จะผลิต ใช้เปิด BOM |
| BOM | สูตรว่าสินค้า 1 ชิ้นใช้วัตถุดิบอะไรเท่าไหร่ (`usage`) |
| kit list | เอกสารเบิกของจากลูกค้า นำเข้าเพื่อสร้างรายการจ่ายออก |
| ปิดยอด PO | ตรวจกระทบยอดจบแล้ว ล็อกไม่ให้คีย์เพิ่ม |
| คิวเคลม | รายการที่รับจริงไม่ตรงกับเอกสาร ต้องเคลมกับผู้ส่ง |
| กระทบยอด (`poRecon`) | เทียบ รับแล้ว / จ่ายแล้ว / ตามสูตร BOM ของ PO ใบนั้น |
